# Vulnerable demonstration API

This project contains deliberate security mistakes used to demonstrate and evaluate
CheckUp. The credential is fake, and the application must not be deployed or used as
a starting point for a real service.

Scan this folder from the CheckUp dashboard to see examples of command injection,
dynamic code execution, SQL injection, path traversal, unsafe deserialization,
disabled TLS verification, and a hardcoded credential.

