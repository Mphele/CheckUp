# Corrected demonstration API

This example mirrors the routes in `examples/vulnerable_api` using safer patterns. It
exists to demonstrate how CheckUp findings respond when the relevant code is changed.
It is an evaluation fixture rather than a production-ready application.

